import 'package:flutter/material.dart';

class BookingTreatmentScreen extends StatelessWidget {
  const BookingTreatmentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Booking Treatment Content"),
    );
  }
}
